[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
# E/F: pyflakes + pycodestyle, I: import sorting, UP: pyupgrade, B: bugbear
select = ["E", "F", "I", "UP", "B"]
# B008: calling File()/Cookie()/Depends() in defaults is the FastAPI DI pattern.
ignore = ["B008"]
