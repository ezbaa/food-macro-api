services:
  backend:
    build: .
    container_name: macro-api-backend
    ports:
      - "8080:8080"
    env_file:
      - .env  

  frontend:
    build: ./frontend
    container_name: macro-api-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend